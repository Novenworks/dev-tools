name: DevTools CI

on:
  push:
  pull_request:

jobs:
  validate:
    name: Validate DevTools
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run DevTools tests
        shell: pwsh
        run: ./tests/Test-DevTools.ps1
